services:
  back:
    image: alpine:3.21
    container_name: upcycle-back
    restart: always
    volumes:
      - /mnt/truenas-web/back/api-upagain:/usr/local/bin/api-upagain
      - /mnt/truenas-web/back/.env.production:/root/.env
    working_dir: /root
    command: ["/usr/local/bin/api-upagain"]

  front:
    image: nginx:stable-alpine
    container_name: upcycle-front
    restart: always
    ports:
      - "80:80"
    volumes:
      - /mnt/truenas-web/front:/usr/share/nginx/html
      - ./front/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - back
